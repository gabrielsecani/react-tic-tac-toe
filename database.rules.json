{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "gameState": {
      "$gameId": {
        ".indexOn": ["createdAt"],
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
